# indexer

## installation

### Debian/Ubuntu

    # apt-get install libczmq-dev
    # pip3 install pyzmq --install-option="--zmq=/usr/lib"
    
### Fedora/CentOS/RH

    # yum install zeromq-devel
    # pip3 install pyzmq --install-option="--zmq=/usr/lib"
    